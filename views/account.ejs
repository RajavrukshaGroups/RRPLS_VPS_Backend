<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>
      Accounts Summary - <%= department?.name || "" %> - <%= monthName %> <%=
      year %>
    </title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <style>
      /* Basic reset */
      * {
        box-sizing: border-box;
      }
      html,
      body {
        margin: 0;
        padding: 0;
        font-family: "Helvetica Neue", Arial, sans-serif;
        color: #111;
        background: #fff;
      }

      /* Page container with visible border for print/PDF */
      .page {
        width: 820px;
        margin: 14px auto;
        padding: 14px;
        border: 3px solid #000;
        background: #fff;
      }

      /* Header */
      .header {
        display: flex;
        align-items: center;
        gap: 12px;
      }
      .logo {
        width: 140px;
        height: 48px;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      .logo img {
        max-width: 100%;
        max-height: 48px;
        object-fit: contain;
        display: block;
      }
      .company-info {
        flex: 1;
        text-align: left;
      }
      .company-info .name {
        font-size: 18px;
        font-weight: 800;
        letter-spacing: 0.4px;
      }
      .company-info .addr {
        font-size: 12px;
        margin-top: 4px;
        color: #444;
      }

      .title {
        margin-top: 12px;
        text-align: center;
        font-weight: 900;
        font-size: 16px;
        padding: 8px 6px;
        border-bottom: 2px solid #000;
      }

      /* meta row */
      .meta {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-top: 10px;
        margin-bottom: 8px;
        font-size: 13px;
      }
      .meta .left {
        font-weight: 700;
      }
      .meta .right {
        text-align: right;
        color: #333;
        font-size: 13px;
      }

      /* table */
      .sheet {
        width: 100%;
        border-collapse: collapse;
        margin-top: 8px;
        font-size: 13px;
        table-layout: fixed;
      }

      .sheet thead th {
        background: transparent;
        font-weight: 800;
        padding: 8px;
        text-align: left;
        border-bottom: 2px solid #000;
      }

      .sheet th.col-sno {
        width: 6%;
      }
      .sheet th.col-emp {
        width: 22%;
      }
      .sheet th.col-design {
        width: 16%;
      }
      .sheet th.col-days {
        width: 12%;
        text-align: right;
      }
      .sheet th.col-gross {
        width: 14%;
        text-align: right;
      }
      .sheet th.col-ded {
        width: 14%;
        text-align: right;
      }
      .sheet th.col-net {
        width: 16%;
        text-align: right;
      }

      .sheet tbody td {
        padding: 8px;
        vertical-align: middle;
        border-bottom: 1px solid #e6e6e6;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
      }

      .sheet tbody tr:nth-child(odd) td {
        background: #fff;
      }
      .sheet tbody tr.total-row td {
        border-top: 2px solid #000;
        font-weight: 900;
        background: #fafafa;
      }

      td.amount {
        text-align: right;
        font-variant-numeric: tabular-nums;
      }

      /* footer summary */
      .summary {
        margin-top: 12px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-weight: 700;
        font-size: 14px;
      }

      .generated {
        font-size: 11px;
        color: #666;
      }

      .footer-note {
        margin-top: 8px;
        font-size: 11px;
        color: #666;
        text-align: center;
      }

      /* keep border visible in print */
      @media print {
        .page {
          border: 3px solid #000;
          margin: 0 auto;
          width: 100%;
          max-width: 820px;
        }
        thead th {
          -webkit-print-color-adjust: exact;
        }
      }

      /* small screens (if previewing in browser) */
      @media (max-width: 880px) {
        .page {
          width: 100%;
          margin: 8px;
          padding: 10px;
        }
        .sheet thead th,
        .sheet tbody td {
          font-size: 12px;
        }
      }
    </style>
  </head>

  <body>
    <div class="page" role="document" aria-label="Accounts summary">
      <div class="header" role="banner">
        <div class="logo" aria-hidden="true">
          <% if (company && company.logo) { %> <% if (typeof company.logo ===
          'string') { %>
          <img src="<%= company.logo %>" alt="<%= company.name %> logo" />
          <% } else if (company.logo.url) { %>
          <img src="<%= company.logo.url %>" alt="<%= company.name %> logo" />
          <% } else { %>
          <!-- no logo -->
          <% } %> <% } %>
        </div>

        <div class="company-info">
          <div class="name"><%= company?.name || "" %></div>
          <div class="addr"><%= company?.address || "" %></div>
        </div>

        <div style="min-width: 150px; text-align: right">
          <div style="font-size: 12px; font-weight: 700">
            <%= department?.name || "" %>
          </div>
          <div style="font-size: 12px; color: #333">
            <%= monthName %> <%= year %>
          </div>
        </div>
      </div>

      <div class="title">ACCOUNTS SUMMARY</div>

      <div class="meta" role="region" aria-label="Report metadata">
        <div class="left">
          Department: <strong><%= department?.name || "-" %></strong>
        </div>
        <div class="right">
          Generated:
          <span class="generated"
            ><%= (generatedAt || new Date()).toLocaleString() %></span
          >
        </div>
      </div>

      <table class="sheet" role="table" aria-label="Accounts table">
        <thead>
          <tr>
            <th class="col-sno">Sl No</th>
            <th class="col-emp">Employee Name</th>
            <th class="col-design">Designation</th>
            <th class="col-days">Total Working Days</th>
            <th class="col-days">Paid Days</th>
            <th class="col-days">LOP Days</th>
            <th class="col-gross">Gross</th>
            <th class="col-ded">Deductions</th>
            <th class="col-net">Net Pay</th>
          </tr>
        </thead>

        <tbody>
          <% if (!rows || rows.length === 0) { %>
          <tr>
            <td colspan="9" style="text-align: center; padding: 18px">
              No records for <%= monthName %> <%= year %>
            </td>
          </tr>
          <% } else { %> <% rows.forEach(function(r, idx){ %>
          <tr>
            <td><%= idx + 1 %></td>
            <td style="text-align: left"><%= r.employeeName || "-" %></td>
            <td style="text-align: left"><%= r.designation || "-" %></td>
            <td class="amount"><%= Number(r.totalWorkingDays || 0) %></td>
            <td class="amount"><%= Number(r.paidDays || 0) %></td>
            <td class="amount"><%= Number(r.lopDays || 0) %></td>
            <td class="amount">
              <%= (typeof r.gross === 'number') ? fmt.formatINR(r.gross) :
              (r.gross || "-") %>
            </td>
            <td class="amount">
              <%= (typeof r.totalDeductions === 'number') ?
              fmt.formatINR(r.totalDeductions) : (r.totalDeductions || "-") %>
            </td>
            <td class="amount">
              <%= r.formattedNet || (typeof r.netPay === 'number' ?
              fmt.formatINR(r.netPay) : (r.netPay || "-")) %>
            </td>
          </tr>
          <% }); %> <% } %>

          <!-- totals row -->
          <tr class="total-row" role="row">
            <td colspan="3">Totals</td>
            <td class="amount"><%= Number(totals.totalWorkingDays || 0) %></td>
            <td class="amount"><%= Number(totals.paidDays || 0) %></td>
            <td class="amount"><%= Number(totals.lopDays || 0) %></td>
            <td class="amount">-</td>
            <td class="amount">-</td>
            <td class="amount">
              <%= totals.formattedNetPay || fmt.formatINR(totals.netPay || 0) %>
            </td>
          </tr>
        </tbody>
      </table>

      <div class="summary" aria-hidden="true">
        <div>Prepared by: Accounts</div>
        <div>
          Total Net Pay:
          <strong
            ><%= totals.formattedNetPay || fmt.formatINR(totals.netPay || 0)
            %></strong >
        </div>
      </div>

      <div class="footer-note">
        This is a system generated report. For enquiries contact accounts
        department.
      </div>
    </div>
  </body>
</html>
